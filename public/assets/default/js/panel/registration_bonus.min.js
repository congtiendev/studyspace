/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!**************************************************!*\
  !*** ./resources/js/panel/registration_bonus.js ***!
  \**************************************************/
(function () {
  "use strict";

  var style = getComputedStyle(document.body);
  var primaryColor = style.getPropertyValue('--primary');
  var secondaryColor = style.getPropertyValue('--secondary');
  var warningColor = style.getPropertyValue('--warning');
  function hexToRgb(hex) {
    var rgba = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;
    var result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
    var rgb = '';
    if (result) {
      rgb = "rgb(".concat(parseInt(result[1], 16), ",").concat(parseInt(result[2], 16), ",").concat(parseInt(result[3], 16), ")");
      if (rgba) {
        rgb = "rgba(".concat(parseInt(result[1], 16), ",").concat(parseInt(result[2], 16), ",").concat(parseInt(result[3], 16), ", ").concat(rgba, ")");
      }
    }
    return rgb;
  }
  function pieChart($el, labels, datasets, colors) {
    new Chart($el, {
      type: 'doughnut',
      data: {
        labels: labels,
        datasets: [{
          label: '',
          data: datasets,
          borderWidth: 0,
          borderColor: '',
          backgroundColor: colors,
          pointBackgroundColor: '#fff',
          pointBorderColor: '#6777ef',
          pointRadius: 4,
          fill: true
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        segmentShowStroke: false,
        legend: {
          display: false
        }
      }
    });
  }
  window.makePieChart = function (elId, labels, percent) {
    var bodyEl = document.getElementById(elId).getContext('2d');
    var notComplete = 100 - percent;
    var colors = percent === 100 ? [primaryColor, '#f7f7f7'] : [warningColor, '#f7f7f7'];
    pieChart(bodyEl, labels, [percent, notComplete], colors);
  };
})(jQuery);
/******/ })()
;